# Evidence placeholder

Do not add fabricated output here.

Populate this directory only after a live Azure run with:

- baseline DNS evidence from `vm-client02`
- failed lookup evidence after deleting `link-vnet-lab02`
- recovered lookup evidence after recreating the link
- `az network watcher test-connectivity` results
- any supporting operator notes that cite real command timestamps
